/**Write a function that takes
 *  a string as an argument and returns its length. */
function getStringLenght(str) {
  const length = str.length;
  return length;
}
console.log(`String Lenght: ${getStringLenght("Vikash Kushwaha")}`);
